我先读前端文件结构, 并问几个基本问题:

api: 用ts写的, 如何和项目联动

config: 配置文件, 如何被调用?我看也是ts

pages: 三个, 是放页面文件吗, 内部是vue代码, vue是前端框架

static: 放静态文件

stores: 也是放ts文件, 作用是在存储的时候需要调用的文件吗

types: 完全猜不出来了

App.vue, 相当于flask中的app.py

env.d.ts, 不知道, 环境变量?

main.ts, 也不知道

剩下两个json, 也不知道.

或许我需要先学typescript.
